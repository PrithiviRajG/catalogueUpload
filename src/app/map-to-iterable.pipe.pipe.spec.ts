import { MapToIterable.PipePipe } from './map-to-iterable.pipe.pipe';

describe('MapToIterable.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new MapToIterable.PipePipe();
    expect(pipe).toBeTruthy();
  });
});
